import { LogOut, Search, Upload, UserPlus } from "lucide-react";
import type { Camper } from "../api";
import type { View } from "./Navigation";
import { Avatar } from "./Avatar";

type Props={campName?:string;view:View;query:string;setQuery:(value:string)=>void;results:Camper[];selectCamper:(camper:Camper)=>void;importFile:()=>void;canImport:boolean;userName:string;joinCamp:()=>void;logout:()=>void};
export function AppHeader({campName,view,query,setQuery,results,selectCamper,importFile,canImport,userName,joinCamp,logout}:Props){return <header><div><img className="mobile-header-logo" src="/vault-hq-logo.png" alt="Vault HQ"/><small className="header-camp-name">{campName||"VAULT HQ"}</small><h1>{view}</h1></div><div className="mobile-header-account"><b>{userName}</b><button onClick={joinCamp} aria-label="Join camp"><UserPlus size={17}/></button><button onClick={logout} aria-label="Sign out"><LogOut size={17}/></button></div><div className="search"><Search size={16}/><input aria-label="Search campers" placeholder="Search campers" value={query} onChange={e=>setQuery(e.target.value)}/>{results.length>0&&<div className="results">{results.map(camper=><button key={camper.id} onClick={()=>selectCamper(camper)}><Avatar c={camper}/><span><b>{camper.name}</b><small>{camper.room||"Unassigned"} | {camper.group||"No group"}</small></span></button>)}</div>}</div><button className="primary header-import" onClick={importFile} disabled={!canImport}><Upload size={16}/>Import file</button></header>}
